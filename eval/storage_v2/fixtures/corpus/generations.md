# Immutable generations

A logical source selects one immutable source generation. Atomic activation
changes active_generation_id only after the sealed candidate passes verification.
Membership intervals are source-local and half-open.
