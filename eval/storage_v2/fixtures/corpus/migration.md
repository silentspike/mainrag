# Additive migration

Shadow reads name a candidate generation explicitly. Activation, deployment,
legacy cleanup, and release are separate gates. The old path remains available
until a separately approved destructive cleanup.
