# Content storage

Each content body is addressed by an algorithm-qualified digest. A shared pack
stores verified entries with an entry digest, offset, and stored length. Pack
integrity is checked before publication and after repacking.
