# Lossless graph

A content node digest covers node type, logical length, leaf content identity,
and the ordered typed child sequence. Traversing the artifact root reconstructs
the exact original bytes including whitespace and delimiters.
